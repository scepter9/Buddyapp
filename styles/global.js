import { StyleSheet } from "react-native";
export const globalstyles=StyleSheet.create({
    container:{
        fontFamily: 'inter-light',
        fontSize: 20, // optional, improves visibility
    padding: 10, 
    }
})